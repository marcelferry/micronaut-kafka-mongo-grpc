apiVersion: apps/v1
kind: Deployment
metadata:
  name: "kafka-avro-mongo-grpc"
spec:
  selector:
    matchLabels:
      app: "kafka-avro-mongo-grpc"
  template:
    metadata:
      labels:
        app: "kafka-avro-mongo-grpc"
    spec:
      containers:
        - name: "kafka-avro-mongo-grpc"
          image: "gcr.io/kafka-avro-mongo-grpc/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "kafka-avro-mongo-grpc"
spec:
  selector:
    app: "kafka-avro-mongo-grpc"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080